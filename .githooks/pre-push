#!/bin/sh
# Pre-push hook: verify production build succeeds before pushing
echo "Running build check before push..."
npm run build > /dev/null 2>&1
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Fix errors before pushing."
  exit 1
fi
echo "✅ Build passed. Pushing."
